language: python

python:
  - 3.3

install:
  - sudo apt-get update -qq
  - pip install -r requirements.txt

script:
  - py.test --cov g4s
  - ./build-api-doc.sh

after_success:
  - sudo apt-get install -qq sloccount
  - pip install coviolationsio_app coverage pep8 pip-tools
  - covio
